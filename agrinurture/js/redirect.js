// This function checks if the user is logged in
// function isLoggedIn() {
//     // Example: Check if "isLoggedIn" exists in localStorage and is set to true
//     return localStorage.getItem("isLoggedIn") === "true";
//   }
  
  // This function redirects the user to the login page if they are not logged in
  // function redirectToLogin(event) {
  //   if (!isLoggedIn()) {
  //     event.preventDefault(); // Prevent the default link behavior
  //     window.location.href = "admin/a-login.html"; // Redirect to login page
  //   }
  // }
  
  // // Attach event listeners to all "READ MORE" links
  // document.addEventListener("DOMContentLoaded", function() {
  //   const readMoreLinks = document.querySelectorAll('.read-more');
  //   readMoreLinks.forEach(link => {
  //     link.addEventListener('click', redirectToLogin);
  //   });

  //   const vidButton = document.querySelectorAll('.vidButton');
  //   vidButton.forEach(link => {
  //     link.addEventListener('click', redirectToLogin);
  //   })
  // });

// Simulate the user being logged in
// localStorage.setItem("isLoggedIn", "true");

// Simulate the user being logged out
// localStorage.removeItem("isLoggedIn");
